import { AppDemoDirective } from "../app-demo.directive";

 
describe('AppDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDemoDirective();
    expect(directive).toBeTruthy();
  });
});
